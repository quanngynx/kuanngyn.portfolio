import { ContentType } from "@/common/providers/provider";

const HIGHLIGHTED_CONTENT_PRIORITY = [
    'article',
    'youtube-video',
    'talk',
] as ContentType[];

export default async function BlogPage() {

    return (
        <></>
    );
}
